# you shouldn't commit these into source control
# these are the default directory names, adjust/add to fit your needs
target/
dbt_packages/
logs/
dbt_internal_packages/

# secrets — local connection profile and raw credentials, never commit
profiles.yml
supabase.txt
.user.yml
Projet Final - LocalBike.pdf

# local python env for dbt-core (fusion can't do postgres)
.venv/
